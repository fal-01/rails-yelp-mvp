
  <h1>Restaurants</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %> - <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary m-2"%></li>
    <% end %>
  </ul>
